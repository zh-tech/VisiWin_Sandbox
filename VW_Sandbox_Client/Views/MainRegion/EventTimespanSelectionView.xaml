<vw:View x:Class="HMI.EventTimespanSelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vw="http://inosoft.com/visiwin7"
    mc:Ignorable="d"
    d:DesignWidth="550" d:DesignHeight="500">

    <d:View.DataContext>
        <vw:EventViewSettingsContext/>
    </d:View.DataContext>

    <Grid x:Name="LayoutRoot" Background="{DynamicResource ApplicationBackgroundBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <vw:ComboBox Margin="5,0,5,0" VerticalAlignment="Top"
                     VerticalContentAlignment="Top"
                     MinHeight="25"
                     ItemsSource="{Binding TimeSpanFilterTypes}"
                     SelectedIndex="{Binding SelectedTimeSpanFilterTypeIndex}" MaxDropDownHeight="367" />

        <StackPanel Grid.Row="1" Margin="5,0,5,0" >

            <vw:DateTimeVarIn LocalizableLabelText="@Events.EventFilter.From" 
                              Value="{Binding MinTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              Margin="0,10,0,0"
                              IsEnabled="{Binding CustomTimeSpanFilterEnabled}" />

            <vw:DateTimeVarIn LocalizableLabelText="@Events.EventFilter.To"  
                              Value="{Binding MaxTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              Margin="0,10,0,0"
                              IsEnabled="{Binding CustomTimeSpanFilterEnabled}" />

        </StackPanel>

    </Grid> 		
    
</vw:View>